# KUSANYA — core runtime dependencies
# Pinned to major.minor floors; exact resolved versions are recorded after
# `pip freeze` once the environment is verified (see requirements/lock.txt
# once it exists). Kept loose here because the toolchain (Python 3.14) is
# very new and exact patch pins may not have wheels yet on every platform.

Django>=5.1,<6.0
djangorestframework>=3.15
drf-spectacular>=0.27
django-environ>=0.11
psycopg[binary]>=3.2
redis>=5.0
celery>=5.4
django-celery-beat>=2.7
django-filter>=24.2
gunicorn>=23.0
whitenoise>=6.7
requests>=2.32
qrcode>=8.0
cryptography>=42.0
django-crispy-forms>=2.3
crispy-bootstrap5>=2024.2

